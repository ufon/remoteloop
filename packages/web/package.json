{
    "name": "@remoteloop/web",
    "version": "0.1.1",
    "license": "MIT",
    "scripts": {
        "clear": "rm -rf dist .cache build",
        "start": "parcel public/index.html --open",
        "build": "npm run clear && parcel build public/index.html"
    },
    "devDependencies": {
        "parcel-bundler": "^1.12.4"
    },
    "dependencies": {
        "hammerjs": "^2.0.8",
        "keycode-js": "^3.1.0",
        "socket.io-client": "^3.0.5"
    }
}
