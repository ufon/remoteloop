{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the QR window",
  "windows": ["qr-window"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "opener:default",
    "notification:default"
  ]
}
